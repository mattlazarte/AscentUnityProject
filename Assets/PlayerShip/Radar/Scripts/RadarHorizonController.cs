using UnityEngine;

namespace Ascent.PlayerShip.Radar
{
    public class RadarHorizonController : MonoBehaviour
    {
        private void Update()
        {
            transform.rotation = Quaternion.identity;
        }
    }
}